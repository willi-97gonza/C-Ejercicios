#include <stdio.h>

int main() {
    // Usamos un ciclo for para contar del 1 al 100
    for (int i = 1; i <= 100; i++) {
        printf("%d\n", i); // Imprimimos cada número en una nueva línea
    }

    return 0;
}
